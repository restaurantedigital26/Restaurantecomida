<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <!-- Página de inicio -->
    <url>
        <loc>{{ url_for('landing', _external=True) }}</loc>
        <lastmod>{{ now }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>
    
    <!-- Lista pública de restaurantes -->
    <url>
        <loc>{{ url_for('ver_restaurantes_publico', _external=True) }}</loc>
        <lastmod>{{ now }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>0.9</priority>
    </url>
    
    <!-- Cada restaurante -->
    {% for r in restaurantes %}
    <url>
        <loc>{{ url_for('detalle_restaurante_publico', id=r._id, _external=True) }}</loc>
        <lastmod>{{ r.fecha_actualizacion or now }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
    </url>
    {% endfor %}
</urlset>